(function () { //
	var obj =  function (env, pkg, req, res) {
		let fs = require('fs');
		let me = this;
    me.run = (path) {
    }
    me.showShareFolder = () {
      
    }
	};
	if (typeof module !== 'undefined' && typeof module.exports !== 'undefined') {
		module.exports = obj;
	} 

})();
